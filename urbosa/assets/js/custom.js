$(document).ready(function () {
  initLazyLoadImage()
  initSimpleParallax()
  initLoginPage()

  initSlideOutMenu(
    {
      menu: '#menu-menu-1',
      container: '#menu-container',
      direction: 'left'
    }
  )
})

